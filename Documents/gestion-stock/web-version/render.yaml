services:
  - type: web
    name: gestion-stock
    env: node
    rootDir: web-version
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /